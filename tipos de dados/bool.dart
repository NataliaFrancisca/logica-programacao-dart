void main(){
    final isTheWeekendTheBestSinger = true;
    bool isAdmin; // tipando a variável

    // comparação de valores
    final a = 'Natalia';
    final b = 'Natasha';
    final isTheSameName = a == b;

    // negação do valor
    final sheIsAdmin = true;
	final sheIsNotAdmin = !sheIsAdmin;

    // operador ternario
    final gradePlayer = 10;
    final iAmAGoodPlayer = gradePlayer >= 7 ? "Yes, It's" : "No, It's not";
}